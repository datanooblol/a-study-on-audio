# Copy this file to .env and fill in values before running docker compose.

# Required by the diarization module only (pyannote.audio's pretrained
# pipeline is gated on Hugging Face). See practices/diarization/README.md
# for the exact steps to get one.
HF_TOKEN=

# Optional overrides
WHISPER_MODEL=base
