[project]
name = "capstone"
version = "0.1.0"
description = "Lesson 11: chains the other modules' HTTP APIs into one pipeline"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32",
]

[tool.uv]
package = false
